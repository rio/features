{
    "name": "vcluster",
    "id": "vcluster",
    "version": "1.0.1",
    "description": "Virtual Kubernetes Clusters that run inside regular namespaces. Currently only supports versions from 0.15.4 and above.",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of vcluster to install. Accepts versions with or without the 'v' prefix."
        }
    }
}
